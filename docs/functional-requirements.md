# Functional Requirements

This document defines the core functional requirements for the TODO app.

## Core Requirements

- The user can add a task.
- The user can add a due date to a task.
- A task can be edited.
- Tasks have checkbox controls to mark them as done.
- Once a user marks a task as done, the task text is displayed with a strikethrough.
